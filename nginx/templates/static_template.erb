server {
    listen <%= port %>;
    <% if ipv6 -%>
        listen [::]:<%= port%> <% if default_server%> default_server ipv6only=on<% end%>;
    <% end -%>
    server_name <%= domain %>;

    include error/error.conf;

    location / {
        root <%= directory %>;
    }

}
