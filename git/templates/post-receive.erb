#!/bin/bash

git --work-tree='<%= working_path %>' \
    --git-dir='<%= bare_path %>' \
    checkout -f deploy

<% if service -%>
# Reload if running, stop otherwise
if ! sudo reload <%= service %>; then
    sudo start <%= service %>
fi
<% end -%>
